{
  "name": "@nirapod-audit/workspace",
  "private": true,
  "workspaces": [
    "packages/protocol",
    "packages/core",
    "apps/cli"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "test": "bun test",
    "audit": "bun run apps/cli/src/index.tsx",
    "dev": "bun run --filter '@nirapod-audit/cli' dev"
  },
  "devDependencies": {
    "typescript": "^6.0.2"
  }
}
